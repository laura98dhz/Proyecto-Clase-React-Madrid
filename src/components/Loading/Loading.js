import loading from "../Recursos/img/loading.gif";

export default function Loading() {
  return (
    <img
      style={{
        width: "70px",
      }}
      src={loading}
      alt="Loading..."
    />
  );
}
